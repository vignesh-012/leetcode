class Solution {
    public int finalValueAfterOperations(String[] op) {
        int X=0;
        for(int i=0;i<op.length;i++)
        {
            if(op[i].equals("++X")) X++;
            else if(op[i].equals("X++")) X++;
            else X--;
        }
        return X;
    }
}