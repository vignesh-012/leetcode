class Solution {
    public int scoreOfString(String s) {
        int su=0;
        for(int i=0;i<s.length()-1;i++)
        {
            char ch=s.charAt(i);
            char ch1=s.charAt(i+1);
             su=su+Math.abs((ch+'0')-(ch1+'0'));
        }return su;
        
    }
}